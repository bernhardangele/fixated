% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_asc.R
\name{read_asc}
\alias{read_asc}
\title{Read an EyeLink ASC file}
\usage{
read_asc(
  path,
  eyes = c("L", "R"),
  sample_cols = character(0),
  eye_tracker = "eyelink_opensesame",
  trial_start_pattern = NULL,
  trial_end_pattern = NULL,
  parse_vars = TRUE,
  char_pattern =
    "^MSG\\\\\\\\t\\\\\\\\d+\\\\\\\\s+TRIAL\\\\\\\\s+(\\\\\\\\d+)\\\\\\\\s+ITEM\\\\\\\\s+(\\\\\\\\d+)\\\\\\\\s+WORD\\\\\\\\s+(\\\\\\\\d+)\\\\\\\\s+CHAR\\\\\\\\s+(\\\\\\\\d+)\\\\\\\\s+(\\\\\\\\S+)\\\\\\\\s+(\\\\\\\\d+)"
)
}
\arguments{
\item{path}{Character scalar.  Full path to the \code{.asc} file.}

\item{eyes}{Character vector.  Which eyes to return.  One or both of
\code{"L"} and \code{"R"}.  Defaults to \code{c("L", "R")} (both).}

\item{sample_cols}{Character vector.  Additional MSG-based columns to
parse into the samples table.  Currently unused; reserved for future
extension.}

\item{eye_tracker}{Character scalar.  Shortcut for common eye-tracker
setups that presets \code{trial_start_pattern} and \code{trial_end_pattern}.
Currently supported values are \code{"eyelink_opensesame"} (default) and
\code{"custom"}.  For \code{"eyelink_opensesame"}, the trial start is detected
from \verb{TRIAL … ITEM … WORD 1} ROI messages written by OpenSesame, and
the trial end from \code{END} lines written by the EyeLink host.  Set to
\code{"custom"} and supply \code{trial_start_pattern}/\code{trial_end_pattern} for
other setups.}

\item{trial_start_pattern}{Character scalar or \code{NULL}.  A regular
expression that matches the \strong{first line} of each trial.  When
non-\code{NULL}, overrides the default for \code{eye_tracker}.  Pass \code{NULL} (the
default) to use the pattern implied by \code{eye_tracker}.}

\item{trial_end_pattern}{Character scalar or \code{NULL}.  A regular expression
that matches the \strong{last line} (or a line within) each trial.  When
non-\code{NULL}, overrides the default for \code{eye_tracker}.}

\item{parse_vars}{Logical scalar.  When \code{TRUE} (default) and
\code{eye_tracker = "eyelink_opensesame"}, OpenSesame variable messages of
the form \verb{MSG … var KEY VALUE} with a single-word value are parsed and
added as columns to \code{trial_db}.  Set to \code{FALSE} to skip this step.}

\item{char_pattern}{Character scalar or \code{NULL}. A regular expression that matches
character boundary messages. Defaults to
\code{"^MSG\\\\\\\\t\\\\\\\\d+\\\\\\\\s+TRIAL\\\\\\\\s+(\\\\\\\\d+)\\\\\\\\s+ITEM\\\\\\\\s+(\\\\\\\\d+)\\\\\\\\s+WORD\\\\\\\\s+(\\\\\\\\d+)\\\\\\\\s+CHAR\\\\\\\\s+(\\\\\\\\d+)\\\\\\\\s+(\\\\\\\\S+)\\\\\\\\s+(\\\\\\\\d+)"}.
The pattern must contain exactly 5 or 6 capture groups: 6 groups for \code{trial_nr},
\code{sentence_nr}, \code{word_id}, \code{char_id}, \code{char}, \code{x_end}; 5 groups for the same but
omitting \code{word_id}.}
}
\value{
A named list with elements:
\describe{
\item{\code{samples}}{A \link[tibble:tibble]{tibble} with columns
\code{time} (integer, ms), \code{x} (double, pixels), \code{y} (double, pixels),
\code{pupil} (double), \code{eye} (character, \code{"L"} or \code{"R"}).  When trial
information is found \code{trial_nr} and \code{sentence_nr} columns are also
included.}
\item{\code{events}}{A \link[tibble:tibble]{tibble} with columns \code{type}
(\code{"FIXATION"}, \code{"SACCADE"}, or \code{"BLINK"}), \code{eye}, \code{start_time},
\code{end_time}, \code{duration}, \code{x_start}, \code{y_start}, \code{x_end}, \code{y_end},
\code{avg_x}, \code{avg_y}, \code{avg_pupil}.  When trial information is found
\code{trial_nr} and \code{sentence_nr} columns are also included.}
\item{\code{word_boundaries}}{A \link[tibble:tibble]{tibble} with columns
\code{trial_nr}, \code{sentence_nr}, \code{word_id}, \code{word}, \code{x_start}, \code{x_end},
\code{y_start}, \code{y_end}, parsed from \verb{TRIAL … ITEM … WORD …}
messages written by OpenSesame.  \code{NULL} when no such messages are
found.}
\item{\code{character_boundaries}}{A \link[tibble:tibble]{tibble} with columns
\code{trial_nr}, \code{sentence_nr}, \code{word_id}, \code{char_id}, \code{char}, \code{x_end},
parsed from character-level ROI messages. \code{NULL} when no such messages
are found.}
\item{\code{calibration}}{A \link[tibble:tibble]{tibble} with columns \code{eye},
\code{quality}, \code{avg_error}, \code{max_error}, \code{offset_deg}, \code{x_offset},
\code{y_offset}, parsed from \verb{!CAL VALIDATION} messages.  \code{NULL} when
no such messages are found.}
\item{\code{trial_db}}{A \link[tibble:tibble]{tibble} with one row per trial
and columns \code{trial_nr}, \code{sentence_nr}, \code{t_trial_start},
\code{t_recording_start}, \code{t_gaze_target_on}, \code{t_gaze_target_off},
\code{t_display_on}, \code{t_display_off}, \code{t_trial_end}, \code{has_display_off}.
When \code{parse_vars = TRUE} and \code{eye_tracker = "eyelink_opensesame"},
additional columns are appended for each OpenSesame variable whose
value is a simple scalar (no spaces).  \code{NULL} when no trial
structure is detected.}
}
}
\description{
Parses an EyeLink ASC file (converted from EDF with \code{edf2asc}) and returns
a named list with data frames for samples, events, and (when present)
word boundary, calibration, and trial information.

Both \strong{monocular} (single eye, 4 data columns) and \strong{binocular} (both
eyes, 7 data columns) sample formats are supported.  Binocular files are
automatically detected from the \code{START} header line and produce two rows
per sample timestamp—one for each eye.

The coordinate system origin follows the EyeLink convention: \strong{(0, 0) is
the top-left corner} of the display, with x increasing rightward and y
increasing downward.
}
\examples{
asc_file <- system.file("extdata", "example.asc", package = "fixated")
if (file.exists(asc_file)) {
  result <- read_asc(asc_file)
  head(result$samples)
  head(result$events)
}

# Binocular OpenSesame-style file with word boundaries and trial structure
asc_bino <- system.file("extdata", "sub_1_example.asc", package = "fixated")
if (file.exists(asc_bino)) {
  result <- read_asc(asc_bino)
  head(result$samples)
  head(result$word_boundaries)
  head(result$calibration)
  print(result$trial_db)
  # Get samples and events for trial 0
  trial0 <- get_trial(result, 0L)
}
}
