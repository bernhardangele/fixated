% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_word_info.R
\name{assign_word_info}
\alias{assign_word_info}
\title{Assign Word Information to Fixations}
\usage{
assign_word_info(fixations, word_boundaries, sentence_left_x_boundary = 125)
}
\arguments{
\item{fixations}{A data frame of fixations containing at least a column
named \code{x} (horizontal gaze position in pixels).}

\item{word_boundaries}{A data frame of word boundary information with
columns \code{word_id} (integer), \code{word} (character), and
\code{x_end} (numeric, pixels).  Typically the output of
\code{\link[=read_roi]{read_roi()}} or a post-processed \code{word_boundaries} element
returned by \code{\link[=read_asc]{read_asc()}}.}

\item{sentence_left_x_boundary}{Numeric scalar.  X-coordinate (pixels)
of the left edge of the sentence area.  Fixations to the left of this
value receive \code{word_id = -1}.  Defaults to \code{125}.}
}
\value{
A copy of \code{fixations} with two new columns added:
\describe{
\item{\code{word_id}}{Integer word number, or \code{-1} for fixations before
the sentence start, or \code{-99} for fixations past the last word.}
\item{\code{word}}{Character word text, or \code{NA} for out-of-sentence
fixations.}
}
The columns \code{sentence_nr} and \code{trial_nr} are also set from the first row
of \code{word_boundaries} when those columns are present there.
}
\description{
Adds word-level information (word number and word text) to each fixation in
a data frame of fixations based on word boundary positions.  A
fixation is assigned to the word whose right boundary is the first one to
the right of the fixation's x-coordinate.
}
\examples{
fixations <- dplyr::tibble(x = c(50, 160, 300, 2000), y = c(540, 540, 540, 540))
word_boundaries <- dplyr::tibble(
  word_id = 1:3,
  word    = c("The", "quick", "fox"),
  x_end   = c(200, 360, 500),
  sentence_nr          = 1L,
  trial_nr             = 1L
)
assign_word_info(fixations, word_boundaries)
}
