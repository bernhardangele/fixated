% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_fixations.R
\name{merge_fixations}
\alias{merge_fixations}
\title{Merge Short Fixations with Adjacent Fixations}
\usage{
merge_fixations(
  df,
  max_duration = 80,
  max_distance = 12,
  rate = 1000,
  add_eyelink_correction = TRUE
)
}
\arguments{
\item{df}{A data frame with (at minimum) columns \code{t_start}, \code{t_end},
\code{duration}, \code{x}, \code{y}.  Additional columns are passed through unchanged.}

\item{max_duration}{Numeric scalar.  Fixations shorter than this value (ms)
are candidates for merging.  Defaults to \code{80}.}

\item{max_distance}{Numeric scalar.  Maximum Euclidean distance (pixels)
between the centres of two adjacent fixations for them to be merged.
Defaults to \code{12}.}

\item{rate}{Numeric scalar.  Eye-tracker sampling rate (Hz).  Used only
when \code{add_eyelink_correction = TRUE} to add half a sample duration to
the recomputed durations (EyeLink adds this correction internally).
Defaults to \code{1000}.}

\item{add_eyelink_correction}{Logical.  When \code{TRUE}, adds \code{1000 / rate} ms
to the recomputed duration of merged fixations to replicate EyeLink's
internal timing convention.  Defaults to \code{TRUE}.}
}
\value{
A modified copy of \code{df} with merged fixations.  A logical column
\code{merged} is added (or overwritten) indicating whether each fixation was
involved in a merge.
}
\description{
Merges fixations whose duration is below \code{max_duration} ms with either the
preceding or the subsequent fixation, provided the two fixations are no more
than \code{max_distance} pixels apart.  When both neighbours qualify, the
preceding fixation is preferred.
}
\details{
This function is designed for fixation data in the EyeLink column-naming
convention (\code{t_start}, \code{t_end}, \code{x}, \code{y}, \code{duration}).  For the
tidier-API version that works with \code{start_time}, \code{end_time}, \code{avg_x},
\code{avg_y}, see \code{\link[=clean_fixations]{clean_fixations()}}.
}
\examples{
fixations <- dplyr::tibble(
  t_start  = c(100L, 400L, 450L, 600L),
  t_end    = c(370L, 440L, 580L, 800L),
  duration = c(270L,  40L, 130L, 200L),
  x        = c(300,  310,  320,  600),
  y        = c(540,  541,  540,  540)
)
merge_fixations(fixations, max_duration = 80, max_distance = 20, rate = 1000,
                add_eyelink_correction = FALSE)
}
