% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_eyelink_saccades.R
\name{get_eyelink_saccades}
\alias{get_eyelink_saccades}
\title{Extract EyeLink-detected saccades from an ASC events table}
\usage{
get_eyelink_saccades(events)
}
\arguments{
\item{events}{A \link[tibble:tibble]{tibble} as returned in the \code{events}
element of \code{\link{read_asc}}, with columns \code{type}, \code{eye},
\code{start_time}, \code{end_time}, \code{duration}, \code{x_start}, \code{y_start},
\code{x_end}, \code{y_end}.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per EyeLink-detected
saccade and columns \code{start_time}, \code{end_time}, \code{duration}, \code{x_start},
\code{y_start}, \code{x_end}, \code{y_end}, \code{eye}.  The column \code{x_end} is the
\strong{landing x position} of the saccade (horizontal gaze coordinate at
saccade offset), and \code{y_end} is the corresponding landing y position.
If \code{trial_nr} or \code{sentence_nr} columns are present in \code{events}, they
are preserved in the output.  The tibble is sorted by \code{start_time}.
}
\description{
The EyeLink host computer records saccade events as \code{ESACC} lines in the
ASC file.  \code{\link{read_asc}} parses these into the \code{events} tibble
with \code{type == "SACCADE"}.

This function extracts those rows and returns them in a convenient tibble
that includes the \strong{landing x position} (\code{x_end}) and landing y position
(\code{y_end}) of each saccade, i.e. the gaze coordinates at the end of the
saccade.
}
\examples{
asc_file <- system.file("extdata", "sub_1_example.asc", package = "fixated")
if (file.exists(asc_file)) {
  result   <- read_asc(asc_file)
  saccades <- get_eyelink_saccades(result$events)
  head(saccades)
}
}
