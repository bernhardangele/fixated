% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_trial_info_from_msg.R
\name{get_trial_info_from_msg}
\alias{get_trial_info_from_msg}
\title{Extract Trial Information from ASC Message Lines}
\usage{
get_trial_info_from_msg(text_vector, regex_pattern, multiple_allowed = FALSE)
}
\arguments{
\item{text_vector}{A character vector of MSG lines (one line per element).}

\item{regex_pattern}{A regular expression with \strong{exactly one capture
group} \verb{(…)} that matches the value to be extracted.}

\item{multiple_allowed}{Logical.  If \code{FALSE} (default), throws an error
when the pattern matches more than one line.  If \code{TRUE}, returns the
first match and emits a warning.}
}
\value{
The content of the capture group as a character scalar, or \code{NA}
(with a warning) when no line matches.
}
\description{
Searches a character vector of MSG lines for a regular expression pattern
with exactly one capture group and returns the captured content.  Designed
for extracting variable values written into EyeLink ASC files by
OpenSesame or similar stimulus-presentation software.
}
\examples{
msgs <- c(
  "MSG\t1000 var subject_nr 42",
  "MSG\t1001 var condition high",
  "MSG\t1002 DISPLAY ON"
)
get_trial_info_from_msg(msgs, "MSG\\\\t\\\\d+ var subject_nr (\\\\d+)")
get_trial_info_from_msg(msgs, "MSG\\\\t(\\\\d+) DISPLAY ON")
}
