% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trials_shiny_linked.R
\name{plot_trials_shiny_linked}
\alias{plot_trials_shiny_linked}
\title{Interactive Shiny app with linked fixation highlighting (WebGL)}
\usage{
plot_trials_shiny_linked(
  asc_result = NULL,
  samples = NULL,
  fixations = NULL,
  rois = NULL,
  measures = NULL,
  roi = NULL,
  trial_db = NULL,
  chars = NULL,
  launch.browser = TRUE
)
}
\arguments{
\item{asc_result}{A named list as returned by \code{\link{read_asc}},
containing at minimum \code{samples} and \code{events}.  If you prefer not to use
\code{read_asc}, you may set this to \code{NULL} and provide the other data frames
individually.}

\item{samples}{A data frame of raw gaze samples.}

\item{fixations}{A data frame of detected fixations.}

\item{rois}{A data frame of word regions of interest. Takes precedence over
\code{asc_result$word_boundaries}.}

\item{measures}{A data frame of word measures. If provided, the app will not
recompute measures internally.}

\item{roi}{A data frame of word regions of interest as returned by
\code{\link{read_roi}}. Alias for \code{rois}.}

\item{trial_db}{A data frame containing trial metadata (e.g. \code{t_display_on}).}

\item{chars}{A data frame of character regions of interest. Takes precedence
over \code{asc_result$character_boundaries}.}

\item{launch.browser}{Logical.  Passed to
\code{\link[shiny]{shinyApp}}'s \code{launch.browser} option.  Defaults to
\code{TRUE}.}
}
\value{
Invisibly, the \pkg{shiny} app object (returned by
\code{\link[shiny]{shinyApp}}).  The app is also launched immediately
(unless \code{launch.browser = FALSE}).
}
\description{
Opens a \pkg{shiny} application that lets you browse every trial in an
experiment with \strong{linked highlighting}: clicking a fixation marker in the
plot highlights its associated gaze samples and word region of interest, and
synchronises the data tables below the plot.  Bidirectional linking means
selecting a row in the fixation table also highlights it in the plot.
}
\details{
\subsection{Linked Highlighting}{

Click any fixation marker in the plot to:
\enumerate{
\item Highlight the raw gaze samples belonging to that fixation (blue,
full opacity) while dimming all other samples.
\item Highlight the word ROI that the fixation falls inside (orange
border and fill).
\item Select the corresponding row in the fixation table, word measures
table, and samples table.
}
The linking is \strong{bidirectional}: selecting a row in the fixation table
highlights it in the plot as well.
}

\subsection{Controls}{

Same as \code{\link{plot_trials_shiny_fast}}: trial/sentence selectors,
eye toggle, display-window filter, overlay checkboxes, animation slider,
background image upload, and optional data tables.
}

\subsection{Required packages}{

\code{shiny}, \code{plotly}, \code{DT}, and \code{shinyjs} must be installed.
}
}
\examples{
\dontrun{
asc_file <- system.file("extdata", "sub_1_example.asc", package = "fixated")
if (file.exists(asc_file)) {
  result <- read_asc(asc_file)
  plot_trials_shiny_linked(result)
}
}
}
