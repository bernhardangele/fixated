% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_roi.R
\name{read_roi}
\alias{read_roi}
\title{Read word regions of interest from a CSV file}
\usage{
read_roi(
  path,
  col_map = c(trial = "trial", word_id = "word_id", word = "word", x_start = "x_start",
    x_end = "x_end", y_start = "y_start", y_end = "y_end"),
  ...
)
}
\arguments{
\item{path}{Character scalar.  Path to the CSV file.}

\item{col_map}{Named character vector mapping canonical column names to the
actual column names present in the CSV.  Canonical names and defaults:
\itemize{
\item \code{"trial"} → \code{"trial"} – trial/sentence identifier (\strong{required}).
\item \code{"word_id"} → \code{"word_id"} – word index within trial (\strong{required}).
\item \code{"word"} → \code{"word"} – word text string (optional).
\item \code{"x_start"} → \code{"x_start"} – left edge of ROI in pixels (\strong{required}).
\item \code{"x_end"} → \code{"x_end"} – right edge of ROI in pixels (\strong{required}).
\item \code{"y_start"} → \code{"y_start"} – top edge of ROI in pixels (\strong{required}).
\item \code{"y_end"} → \code{"y_end"} – bottom edge of ROI in pixels (\strong{required}).
}}

\item{...}{Additional arguments forwarded to \code{\link[readr:read_delim]{readr::read_csv()}}.}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{trial}, \code{word_id},
\code{x_start}, \code{x_end}, \code{y_start}, \code{y_end}, and (if present) \code{word}, all
with canonical names.  Sorted by \code{trial}, then \code{word_id}.
}
\description{
Reads a CSV file that describes the rectangular regions of interest (ROIs)
for individual words within sentences/trials and returns a standardised
\link[tibble:tibble]{tibble}.

Each row in the file represents one word.  The ROI is defined by the
pixel coordinates of its bounding box.  The coordinate system origin
follows the EyeLink convention: \strong{(0, 0) is the top-left corner} of the
display, with x increasing rightward and y increasing downward.
}
\examples{
roi_file <- system.file("extdata", "example_roi.csv", package = "fixated")
if (file.exists(roi_file)) {
  roi <- read_roi(roi_file)
  head(roi)
}
}
