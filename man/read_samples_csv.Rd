% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_samples_csv.R
\name{read_samples_csv}
\alias{read_samples_csv}
\title{Read raw eye-tracking samples from a CSV file}
\usage{
read_samples_csv(
  path,
  col_map = c(time = "time", x = "x", y = "y", pupil = "pupil", eye = "eye", trial =
    "trial", participant = "participant"),
  eyes = c("L", "R"),
  ...
)
}
\arguments{
\item{path}{Character scalar.  Path to the CSV file.}

\item{col_map}{Named character vector mapping canonical column names to the
actual column names present in the CSV.  The canonical names are:
\itemize{
\item \code{"time"} – sample timestamp (integer, milliseconds).
\item \code{"x"} – horizontal gaze position (numeric, pixels).
\item \code{"y"} – vertical gaze position (numeric, pixels).
\item \code{"pupil"} – pupil size (numeric; optional, pass \code{NA} to omit).
\item \code{"eye"} – which eye (\code{"L"} or \code{"R"}; optional, pass \code{NA} to omit).
\item \code{"trial"} – trial identifier (optional, pass \code{NA} to omit).
\item \code{"participant"} – participant identifier (optional, pass \code{NA} to omit).
}

Defaults to \code{c(time = "time", x = "x", y = "y", pupil = "pupil", eye = "eye", trial = "trial", participant = "participant")}.}

\item{eyes}{Character vector.  Which eye values to retain.  Defaults to
\code{c("L", "R")}.  Ignored if the \code{"eye"} mapping is \code{NA}.}

\item{...}{Additional arguments forwarded to \code{\link[readr:read_delim]{readr::read_csv()}}.}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{time} (integer), \code{x}
(double), \code{y} (double), and any of \code{pupil}, \code{eye}, \code{trial},
\code{participant} that were present in the source file.
}
\description{
Reads a CSV file containing raw gaze samples exported from an eye tracker
and returns a standardised \link[tibble:tibble]{tibble}.  The function expects
at minimum columns for time, x-coordinate, and y-coordinate (column names
are configurable via the \code{col_map} argument).

The coordinate system origin follows the EyeLink convention: \strong{(0, 0) is
the top-left corner} of the display, with x increasing rightward and y
increasing downward.
}
\examples{
csv_file <- system.file("extdata", "example_samples.csv", package = "fixated")
if (file.exists(csv_file)) {
  samples <- read_samples_csv(csv_file)
  head(samples)
}
}
