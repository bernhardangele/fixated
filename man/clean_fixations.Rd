% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_fixations.R
\name{clean_fixations}
\alias{clean_fixations}
\title{Clean and merge a fixation sequence}
\usage{
clean_fixations(
  fixations,
  min_duration = 80,
  max_duration = 1200,
  x_bounds = c(-Inf, Inf),
  y_bounds = c(-Inf, Inf),
  merge_distance = 40,
  merge_max_gap = 75,
  trial_col = "trial",
  eye_col = "eye"
)
}
\arguments{
\item{fixations}{A data frame of fixations with (at minimum) columns
\code{start_time}, \code{end_time}, \code{duration}, \code{avg_x}, and \code{avg_y}.  May also
contain \code{trial} and \code{eye} columns.}

\item{min_duration}{Numeric scalar.  Fixations shorter than this value (ms)
are removed \emph{before} merging.  Defaults to \code{80}.}

\item{max_duration}{Numeric scalar.  Fixations longer than this value (ms)
are removed.  Set to \code{Inf} to disable.  Defaults to \code{1200}.}

\item{x_bounds}{Numeric vector of length 2 \code{c(min, max)}.  Fixations
outside these horizontal bounds (pixels) are removed.  Set to
\code{c(-Inf, Inf)} to disable.  Defaults to \code{c(-Inf, Inf)}.}

\item{y_bounds}{Numeric vector of length 2 \code{c(min, max)}.  Fixations
outside these vertical bounds (pixels) are removed.  Set to
\code{c(-Inf, Inf)} to disable.  Defaults to \code{c(-Inf, Inf)}.}

\item{merge_distance}{Numeric scalar.  Maximum spatial distance (pixels,
Euclidean) between two consecutive fixation centres for them to be
candidates for merging.  Defaults to \code{40}.}

\item{merge_max_gap}{Numeric scalar.  Maximum temporal gap (ms) between
\code{end_time} of fixation \emph{i} and \code{start_time} of fixation \emph{i+1} for
them to be candidates for merging.  Defaults to \code{75}.}

\item{trial_col}{Character scalar or \code{NULL}.  Name of the column
identifying trials.  Merging only occurs within trials.  Defaults to
\code{"trial"}.}

\item{eye_col}{Character scalar or \code{NULL}.  Name of the column identifying
the recorded eye.  Merging only occurs within the same eye.  Defaults to
\code{"eye"}.}
}
\value{
A \link[tibble:tibble]{tibble} of cleaned fixations with the same
columns as \code{fixations}, sorted by \code{start_time} within each group.
The \code{duration} column is recomputed as \code{end_time - start_time} after
merging.  For merged fixations, \code{avg_x} and \code{avg_y} are
duration-weighted means of the contributing fixations.
}
\description{
Post-processes a data frame of fixations (e.g. from \code{detect_fixations()} or
from \code{read_asc()}) by:
\enumerate{
\item \strong{Removing outliers} – fixations whose duration or spatial position
fall outside user-specified bounds.
\item \strong{Merging adjacent fixations} – consecutive fixations (within the same
trial and eye) that are separated by a short saccade and land near the
same location are merged into a single fixation.  This corrects for
"fixation splitting" caused by small blinks or noise.
}
}
\examples{
fixations <- dplyr::tibble(
  start_time = c(0L, 150L, 160L, 400L),
  end_time   = c(120L, 155L, 280L, 600L),
  duration   = c(120L, 5L, 120L, 200L),
  avg_x      = c(300, 305, 308, 700),
  avg_y      = c(400, 402, 401, 400)
)
clean <- clean_fixations(fixations, min_duration = 80, merge_distance = 40)
print(clean)
}
