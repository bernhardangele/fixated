% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trials_shiny.R
\name{plot_trials_shiny}
\alias{plot_trials_shiny}
\title{Launch an interactive Shiny app to visualise eye-tracking trials}
\usage{
plot_trials_shiny(asc_result, roi = NULL, launch.browser = TRUE)
}
\arguments{
\item{asc_result}{A named list as returned by \code{\link{read_asc}},
containing at minimum \code{samples} and \code{events}.  When the list also
includes \code{word_boundaries} and/or \code{trial_db} those are used
automatically.}

\item{roi}{A data frame of word regions of interest as returned by
\code{\link{read_roi}}, or any compatible data frame with columns
\code{word_id}, \code{x_start}, \code{x_end}, \code{y_start}, \code{y_end}.  When supplied it
takes precedence over \code{asc_result$word_boundaries} for boundary and
label overlays.  Pass \code{NULL} (the default) to use
\code{asc_result$word_boundaries} instead.}

\item{launch.browser}{Logical.  Passed to
\code{\link[shiny]{shinyApp}}'s \code{options} list as the \code{launch.browser}
element.  Defaults to \code{TRUE}.}
}
\value{
Invisibly, the \pkg{shiny} app object (returned by
\code{\link[shiny]{shinyApp}}).  The app is also launched immediately
(unless \code{launch.browser = FALSE}).
}
\description{
Opens a \pkg{shiny} application that lets you browse every trial in an
experiment, toggling overlays for raw gaze samples, EyeLink-detected
fixations, and word regions of interest.  The gaze plot is rendered with
\pkg{plotly} so you can hover over individual samples and fixations to see
detailed tooltips, and zoom / pan the view.
}
\details{
\subsection{Controls}{

\describe{
\item{Trial Number}{Select any trial by its \code{trial_nr} (or sequential
index when no \code{trial_nr} column is present). Use the
\strong{Previous} / \strong{Next} buttons to step through trials one by one.}
\item{Eye}{Choose which eye's data to display (\code{L} for left, \code{R} for
right).}
\item{Display window filter}{Optionally restrict samples and fixations
to the period between \code{t_display_on} and \code{t_display_off} as recorded
in \code{asc_result$trial_db}.}
\item{Overlay toggles}{Independent checkboxes to show/hide raw gaze
samples, detected fixations, and word-boundary lines with labels.}
\item{Animate slider}{Replay the trial up to any relative time (ms
from display onset).}
\item{Background image}{Upload a PNG or JPEG screenshot of the stimulus
display for layout verification.}
\item{Data tables}{Optionally show interactive tables of detected
fixations, word measures, and raw samples below the plot.}
}
}

\subsection{Required packages}{

\code{shiny}, \code{plotly}, and \code{DT} must be installed.  The function checks for
their availability at call time and stops with an informative message if
any are missing.
}
}
\examples{
\dontrun{
asc_file <- system.file("extdata", "sub_1_example.asc", package = "fixated")
if (file.exists(asc_file)) {
  result <- read_asc(asc_file)
  plot_trials_shiny(result)
}
}
}
