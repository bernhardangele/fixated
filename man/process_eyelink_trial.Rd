% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_eyelink_trial.R
\name{process_eyelink_trial}
\alias{process_eyelink_trial}
\title{Process a Single Trial Using EyeLink Fixation Events}
\usage{
process_eyelink_trial(
  eyelink_fixations,
  eyelink_blinks,
  trial_words,
  trial_db_for_trial,
  use_eye = "R",
  min_duration_for_elimination = 800,
  max_duration_for_elimination_or_merge = 80,
  max_distance_for_merge = 12,
  sentence_start_x = 125
)
}
\arguments{
\item{eyelink_fixations}{A data frame of EyeLink fixation events for the
trial (typically from the \code{trial_fixations} list returned by
\code{\link[=read_asc]{read_asc()}}).  Must contain columns \code{eye}, \code{t_start}, \code{t_end},
\code{duration}, \code{x}, \code{y}.}

\item{eyelink_blinks}{A data frame of EyeLink blink events for the trial,
or \code{NA} when no blinks were recorded.  When a data frame, must contain
columns \code{eye}, \code{t_start}, \code{t_end}.}

\item{trial_words}{A data frame of word-boundary information for the
trial (e.g., from \code{\link[=get_word_info_from_msg]{get_word_info_from_msg()}}).}

\item{trial_db_for_trial}{A single-row data frame (one trial from
\code{trial_db}) containing at least \code{timestamp_display_on},
\code{timestamp_display_off}, \code{recording_sampling_rate}, \code{subject_nr},
and \code{sentence_number}.}

\item{use_eye}{Character scalar.  Which eye to process (\code{"L"} or \code{"R"}).
Defaults to \code{"R"}.}

\item{min_duration_for_elimination}{Numeric scalar.  Fixations longer than
this value (ms) are flagged as \code{too_long}.  Defaults to \code{800}.}

\item{max_duration_for_elimination_or_merge}{Numeric scalar.  Fixations
shorter than this value (ms) are flagged as \code{too_short} (candidates for
elimination or merging).  Defaults to \code{80}.}

\item{max_distance_for_merge}{Numeric scalar.  Maximum distance (pixels)
between two fixation centres for the short one to be merged with its
neighbour.  Defaults to \code{12}.}

\item{sentence_start_x}{Numeric scalar.  Left x boundary of the sentence
area (pixels).  Defaults to \code{125}.}
}
\value{
A data frame of cleaned fixations with word information added (see
\code{\link[=assign_word_info]{assign_word_info()}}), or \code{NULL} when no fixations remain after
processing.
}
\description{
Processes the EyeLink-detected fixation events for one trial of a reading
experiment.  The function filters fixations to the display-on interval,
marks fixations near blinks, flags short/long fixations, merges short
fixations with adjacent ones, and finally assigns word information from
the trial's word-boundary data.
}
\seealso{
\code{\link[=assign_word_info]{assign_word_info()}}, \code{\link[=merge_fixations]{merge_fixations()}},
\code{\link[=mark_fixations_close_to_blinks]{mark_fixations_close_to_blinks()}}, \code{\link[=calculate_fixation_time_measures]{calculate_fixation_time_measures()}}
}
