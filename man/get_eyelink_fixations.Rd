% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_eyelink_fixations.R
\name{get_eyelink_fixations}
\alias{get_eyelink_fixations}
\title{Extract EyeLink-detected fixations from an ASC events table}
\usage{
get_eyelink_fixations(events)
}
\arguments{
\item{events}{A \link[tibble:tibble]{tibble} as returned in the \code{events}
element of \code{\link{read_asc}}, with columns \code{type}, \code{eye},
\code{start_time}, \code{end_time}, \code{duration}, \code{avg_x}, \code{avg_y}.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per EyeLink-detected
fixation and columns \code{start_time}, \code{end_time}, \code{duration}, \code{avg_x},
\code{avg_y}, \code{avg_pupil}, \code{eye}.  If a \code{trial_nr} column is present in
\code{events}, it is preserved in the output.  The tibble is sorted by
\code{start_time}.
}
\description{
The EyeLink host computer runs its own fixation-detection algorithm and
writes the results as \code{EFIX} lines to the ASC file.  \code{\link{read_asc}}
parses these into the \code{events} tibble with \code{type == "FIXATION"}.

This function extracts those rows and returns them in the same column
format produced by \code{\link{detect_fixations}}, making it easy to
substitute EyeLink fixations for algorithm-detected ones in downstream
analysis steps.
}
\examples{
asc_file <- system.file("extdata", "sub_1_example.asc", package = "fixated")
if (file.exists(asc_file)) {
  result    <- read_asc(asc_file)
  fixations <- get_eyelink_fixations(result$events)
  head(fixations)
}
}
