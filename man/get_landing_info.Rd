% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_landing_info.R
\name{get_landing_info}
\alias{get_landing_info}
\title{Add landing position information to a fixations tibble}
\usage{
get_landing_info(fixations, word_boundaries)
}
\arguments{
\item{fixations}{A \link[tibble:tibble]{tibble} (or data frame) of fixations.
Must contain at least \code{avg_x} (average fixation x position, numeric).
Columns \code{trial_nr}, \code{sentence_nr}, \code{eye}, \code{start_time} / \code{start} are
used when present (see Details).}

\item{word_boundaries}{A \link[tibble:tibble]{tibble} (or data frame) of word
boundaries.  Must contain \code{word_id}, \code{x_start}, and \code{x_end}.  May also
contain a word-text column named \code{word} or \code{word_text} (used for
\code{fixated_char}), and \code{trial_nr} / \code{sentence_nr} join keys.}
}
\value{
The input \code{fixations} tibble with four additional columns:
\code{word_id} (integer), \code{fixation_type} (character), \code{word_proportion}
(double, \verb{[0, 1]}), and \code{fixated_char} (integer).  The row order matches
the input.
}
\description{
Takes a fixations tibble (with an \code{avg_x} column) and a word-boundaries
tibble (with \code{word_id}, \code{x_start}, \code{x_end}, and optionally a word-text
column named \code{word} or \code{word_text}) and returns the fixations tibble with
four new columns:
\itemize{
\item \code{word_id} – the word that the fixation landed on, matched by testing
whether \code{avg_x} falls in \verb{[x_start, x_end]}.  \code{NA} when no word boundary
contains \code{avg_x}.  When multiple words overlap at \code{avg_x}, the word with
the smallest boundary width is chosen; ties are broken by the smallest
\code{x_start}.
\item \code{fixation_type} – \code{"first"} for the first fixation on a given \code{word_id}
within each trial (grouped by \code{trial_nr} if present; also by \code{eye} if
present), \code{"refixation"} for every subsequent fixation on the same word.
Temporal order is determined by \code{start_time} (preferred) or \code{start}.
\code{NA} when \code{word_id} is \code{NA}.
\item \code{word_proportion} – where within the word the fixation landed:
\code{(avg_x - x_start) / (x_end - x_start)}.  Clamped to \verb{[0, 1]} for
slight numeric drift.  \code{NA} when the word width is <= 0 or \code{word_id} is
\code{NA}.
\item \code{fixated_char} – assuming a monospace font, the 1-based character
position that \code{word_proportion} maps to.  For a word of \code{N} characters:
\code{pmin(N, pmax(1, floor(word_proportion * N) + 1))}.  \code{NA} when
\code{word_proportion} or \code{N} is \code{NA} / 0.
}
}
\section{Join keys}{

The function determines how to match fixations to word boundaries:
\enumerate{
\item If \code{trial_nr} is present in \strong{both} tibbles, boundaries are
matched per trial.
\item Otherwise, if \code{sentence_nr} is present in both, boundaries are
matched per sentence.
\item Otherwise, a cross join is performed and a warning is emitted.
In this case every fixation is matched against every word boundary,
so the boundaries must correspond to the same trial/sentence as the
fixations.
}
}

\examples{
fixations <- dplyr::tibble(
  trial_nr   = c(1L, 1L, 1L),
  start_time = c(100L, 300L, 500L),
  avg_x      = c(150, 600, 150)   # word 1, word 2, word 1 again
)
word_boundaries <- dplyr::tibble(
  trial_nr = c(1L, 1L),
  word_id  = c(1L, 2L),
  word     = c("Hello", "world"),
  x_start  = c(100, 500),
  x_end    = c(300, 800)
)
result <- get_landing_info(fixations, word_boundaries)
result[, c("word_id", "fixation_type", "word_proportion", "fixated_char")]
}
