% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blink_overlap_or_near.R
\name{blink_overlap_or_near}
\alias{blink_overlap_or_near}
\title{Evaluate Blink Overlap or Proximity to a Fixation}
\usage{
blink_overlap_or_near(
  fixation_start,
  fixation_end,
  blink_start,
  blink_end,
  t_diff_threshold = 100
)
}
\arguments{
\item{fixation_start}{Numeric.  Start timestamp of the fixation (ms).}

\item{fixation_end}{Numeric.  End timestamp of the fixation (ms).}

\item{blink_start}{Numeric (scalar or vector).  Start timestamp(s) of the
blink(s) (ms).}

\item{blink_end}{Numeric (scalar or vector).  End timestamp(s) of the
blink(s) (ms).}

\item{t_diff_threshold}{Numeric scalar.  Time difference threshold (ms).
A fixation is flagged when either its start is within this many ms of a
blink end, or its end is within this many ms of a blink start.  Defaults
to \code{100}.}
}
\value{
A logical vector of the same length as \code{blink_start} / \code{blink_end},
where \code{TRUE} means the corresponding blink overlaps or is near the
fixation.
}
\description{
Tests whether a fixation and a blink overlap in time or are within a
specified time-difference threshold of each other.  This is used to flag
fixations that may be contaminated by blink artifacts.
}
\examples{
# Fixation 100–200 ms, blink 190–240 ms (overlapping)
blink_overlap_or_near(100, 200, 190, 240)

# Fixation 100–200 ms, blink 250–300 ms (50 ms gap, within threshold=100)
blink_overlap_or_near(100, 200, 250, 300, t_diff_threshold = 100)

# Fixation 100–200 ms, blink 400–500 ms (not near)
blink_overlap_or_near(100, 200, 400, 500, t_diff_threshold = 100)
}
