% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_eyelink_trial_subject.R
\name{calculate_subject_eyelink_measures}
\alias{calculate_subject_eyelink_measures}
\title{Calculate EyeLink-Based Eye-Movement Measures for a Subject}
\usage{
calculate_subject_eyelink_measures(subject, use_eye = "R")
}
\arguments{
\item{subject}{A list with the structure returned by the
\code{read_asc_file()} function (from the original analysis scripts).  The
list must contain:
\describe{
\item{\code{filename}}{Character string – subject identifier.}
\item{\code{trial_db}}{Data frame with one row per trial, including columns
\code{trial_type}, \code{has_display_off}, \code{subject_nr},
\code{recording_sampling_rate}, \code{target_word_nr}, \code{response}, \code{cond},
\code{sentence_number}.}
\item{\code{trial_fixations}}{List of per-trial fixation data frames.}
\item{\code{trial_blinks}}{List of per-trial blink data frames (or \code{NA}).}
\item{\code{trial_words}}{List of per-trial word-boundary data frames.}
}}

\item{use_eye}{Character scalar.  Which eye to use (\code{"L"} or \code{"R"}).
Defaults to \code{"R"}.}
}
\value{
A data frame (one row per word per trial) with columns \code{word_nr},
\code{word}, \code{ffd}, \code{gd}, \code{sfd}, \code{gopast}, \code{tvt}, \code{subject},
\code{target_word_nr}, \code{acceptability}, \code{rate}, and \code{cond}.
}
\description{
Iterates over all experimental trials for a subject, processes each trial's
EyeLink fixation events using \code{\link[=process_eyelink_trial]{process_eyelink_trial()}}, and computes
word-level eye-movement measures with \code{\link[=calculate_fixation_time_measures]{calculate_fixation_time_measures()}}.
}
\details{
Only trials where \code{trial_type == "experimental"} \strong{and}
\code{has_display_off == TRUE} are processed; all other trials return \code{NULL}
and are silently skipped.
}
\seealso{
\code{\link[=process_eyelink_trial]{process_eyelink_trial()}}, \code{\link[=calculate_fixation_time_measures]{calculate_fixation_time_measures()}}
}
