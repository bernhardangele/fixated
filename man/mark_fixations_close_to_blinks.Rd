% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark_fixations_close_to_blinks.R
\name{mark_fixations_close_to_blinks}
\alias{mark_fixations_close_to_blinks}
\title{Mark Fixations Close to Blinks}
\usage{
mark_fixations_close_to_blinks(fixations, blinks, t_diff_threshold = 100)
}
\arguments{
\item{fixations}{A data frame of fixations with columns \code{t_start} and
\code{t_end} (timestamps in ms).}

\item{blinks}{A data frame of blinks with columns \code{t_start} and \code{t_end}
(timestamps in ms).}

\item{t_diff_threshold}{Numeric scalar.  Time-difference threshold (ms)
passed to \code{\link[=blink_overlap_or_near]{blink_overlap_or_near()}}.  Defaults to \code{100}.}
}
\value{
A copy of \code{fixations} with an additional logical column \code{near_blink}
(overwrites the column if it already exists).
}
\description{
Adds a logical column \code{near_blink} to a data frame of fixations indicating
whether each fixation overlaps with or is temporally close to any detected
blink.  Uses \code{\link[=blink_overlap_or_near]{blink_overlap_or_near()}} for the proximity test.
}
\examples{
fixations <- dplyr::tibble(
  t_start = c(100, 500, 900),
  t_end   = c(300, 700, 1100)
)
blinks <- dplyr::tibble(
  t_start = c(280),
  t_end   = c(350)
)
mark_fixations_close_to_blinks(fixations, blinks)
}
