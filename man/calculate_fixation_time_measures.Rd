% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_fixation_time_measures.R
\name{calculate_fixation_time_measures}
\alias{calculate_fixation_time_measures}
\title{Calculate Fixation Time Measures for a Single Trial}
\usage{
calculate_fixation_time_measures(fixations, words)
}
\arguments{
\item{fixations}{A data frame of fixations for a single trial.  Must
contain columns \code{word_id}, \code{duration}, \code{too_short}, \code{too_long},
\code{merged}, \code{near_blink}.}

\item{words}{A data frame of word information for the trial (e.g., from
\code{\link[=get_word_info_from_msg]{get_word_info_from_msg()}}).  Must contain columns \code{word_id}, \code{word},
and \code{x_end}.}
}
\value{
A data frame with one row per word and the following columns:
\describe{
\item{\code{word_id}}{Word number.}
\item{\code{word}}{Word text.}
\item{\code{ffd}}{First Fixation Duration (ms) – duration of the first
fixation on the word during the first pass.}
\item{\code{gd}}{Gaze Duration (ms) – sum of all first-pass fixation
durations on the word.}
\item{\code{sfd}}{Single Fixation Duration (ms) – equal to FFD when the
word received exactly one fixation during the first pass, otherwise
\code{NA}.}
\item{\code{gopast}}{Go-Past Time (ms) – total time from first entering
the word until the gaze exits to the right, including regressions.
Also known as regression-path duration.}
\item{\code{tvt}}{Total Viewing Time (ms) – sum of all fixation durations
on the word across the entire trial.}
}
}
\description{
Computes standard reading-research eye-movement measures (FFD, GD, SFD,
go-past time, TVT) for each word in a trial from a cleaned fixation data
frame that contains word assignments.
}
\details{
This function is designed to work with the output of \code{\link[=process_eyelink_trial]{process_eyelink_trial()}}
or \code{\link[=assign_word_info]{assign_word_info()}}.  The fixation data frame must contain quality flags
(\code{too_short}, \code{too_long}, \code{merged}, \code{near_blink}) that are used to filter
fixations before computing the measures.
}
\examples{
# Minimal example with three words and five fixations
fixations <- dplyr::tibble(
  word_id    = c(1L, 2L, 3L, 2L, 3L),
  duration   = c(200L, 150L, 180L, 120L, 90L),
  too_short  = FALSE,
  too_long   = FALSE,
  merged     = FALSE,
  near_blink = FALSE
)
words <- dplyr::tibble(
  word_id               = 1:3,
  word                  = c("The", "quick", "fox"),
  x_end                 = c(200L, 360L, 500L)
)
calculate_fixation_time_measures(fixations, words)
}
