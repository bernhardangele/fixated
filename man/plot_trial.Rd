% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trial.R
\name{plot_trial}
\alias{plot_trial}
\title{Plot a single eye-tracking trial}
\usage{
plot_trial(
  asc_result,
  trial_nr,
  eye = "R",
  show_samples = TRUE,
  show_fixations = TRUE,
  show_word_boundaries = TRUE,
  show_word_labels = TRUE,
  show_measures = FALSE,
  roi = NULL,
  x_limits = NULL,
  y_limits = NULL,
  bg_image_path = NULL,
  display_window_only = TRUE
)
}
\arguments{
\item{asc_result}{A named list as returned by \code{\link{read_asc}},
containing at minimum \code{samples} and \code{events}.  When the list also
includes \code{word_boundaries} and/or \code{trial_db} those are used
automatically.}

\item{trial_nr}{Integer scalar.  Which trial to visualise.  Must match a
value in the \code{trial_nr} column of \code{asc_result$samples} (added
automatically by \code{\link{read_asc}} when trial structure is
detected).}

\item{eye}{Character scalar.  Which eye's data to plot (\code{"L"} or \code{"R"}).
Defaults to \code{"R"}.}

\item{show_samples}{Logical.  Plot raw gaze samples as semi-transparent
grey points.  Defaults to \code{TRUE}.}

\item{show_fixations}{Logical.  Overlay EyeLink-detected fixations as
red circles (size proportional to fixation duration) connected by a
dashed path.  Defaults to \code{TRUE}.}

\item{show_word_boundaries}{Logical.  Draw dashed vertical lines at word
right-boundary positions.  Requires either \code{asc_result$word_boundaries}
or \code{roi} to be provided.  Defaults to \code{TRUE}.}

\item{show_word_labels}{Logical.  Print word text labels at the horizontal
centre of each word region.  Requires \code{asc_result$word_boundaries} or
\code{roi} to be provided, and both must contain a \code{word} column.  Defaults
to \code{TRUE}.}

\item{show_measures}{Logical.  Annotate each word with its FFD, GD, and
TVT values in milliseconds.  Requires \code{roi} (a full ROI table with
\code{x_start}, \code{x_end}, \code{y_start}, \code{y_end} columns) or
\code{asc_result$word_boundaries} with those columns present.  Defaults to
\code{FALSE}.}

\item{roi}{A data frame of word regions of interest as returned by
\code{\link{read_roi}}, or any compatible data frame with columns
\code{word_id}, \code{x_start}, \code{x_end}, \code{y_start}, \code{y_end}.  When supplied it
takes precedence over \code{asc_result$word_boundaries} for boundary, label,
and measure overlays.  Pass \code{NULL} (the default) to use
\code{asc_result$word_boundaries} instead.}

\item{x_limits}{Numeric vector of length 2 giving the horizontal axis
range in pixels.  When \code{NULL} (default) the range is inferred from the
data with a small margin.}

\item{y_limits}{Numeric vector of length 2 \code{c(min, max)} giving the
vertical axis range in pixels.  When \code{NULL} (default) the range is
inferred from the data with a small margin.  The y-axis is always
displayed in reversed order (origin at top-left) to match the EyeLink
coordinate convention.}

\item{bg_image_path}{Character scalar.  Path to a PNG or JPEG file to
render as a background image (for stimulus-layout verification).
Requires the \pkg{png} package for PNG files or the \pkg{jpeg} package
for JPEG files.  Defaults to \code{NULL} (no background image).}

\item{display_window_only}{Logical.  When \code{TRUE} (default) and
\code{asc_result$trial_db} contains \code{t_display_on} / \code{t_display_off}
columns, samples and fixations outside the display window are removed
before plotting.}
}
\value{
A \pkg{ggplot2} \link[ggplot2:ggplot]{ggplot} object.  Print it,
pass it to \code{ggplot2::ggsave()}, or add further layers with \code{+}.
}
\description{
Creates a \pkg{ggplot2} visualisation of eye-tracking data for a single
trial.  The plot can show raw gaze samples, EyeLink-detected fixations,
word boundary lines, word text labels, and word-level reading measures
(FFD, GD, TVT).  A background image (PNG or JPEG) may optionally be placed
behind the data for stimulus-layout verification.
}
\details{
\subsection{Word boundaries}{
The function looks for ROI / boundary data in the following order:
\enumerate{
\item The explicit \code{roi} argument (when non-\code{NULL}).
\item \code{asc_result$word_boundaries}, which is automatically populated
by \code{\link{read_asc}} from OpenSesame \verb{TRIAL … RIGHT_BOUNDARY}
messages.
}
When the available data contains \code{x_start}, \code{x_end}, \code{y_start}, and
\code{y_end} columns a shaded rectangle is drawn for each word region.  When
only \code{x_end} is available, only vertical boundary lines are drawn.
}
\subsection{Word measures}{
FFD, GD, and TVT are computed internally by calling
\code{\link{compute_eye_measures}} on the EyeLink-detected fixations and
the word ROI.  The measure values are displayed only when
\code{show_measures = TRUE} and a full ROI (with all four boundary columns)
is available.
}
\subsection{Background image}{
The image is drawn as a raster layer beneath all other plot elements via
\code{ggplot2::annotation_raster()}.  It spans the full \code{x_limits} ×
\code{y_limits} extent, so the image should represent the full stimulus
display at the experiment resolution.
}
}
\examples{
asc_file <- system.file("extdata", "sub_1_example.asc", package = "fixated")
if (requireNamespace("ggplot2", quietly = TRUE) && file.exists(asc_file)) {
  result <- read_asc(asc_file)
  p <- plot_trial(result, trial_nr = 0L)
  print(p)
}
}
