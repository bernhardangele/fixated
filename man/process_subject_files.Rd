% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_subject_files.R
\name{process_subject_files}
\alias{process_subject_files}
\title{Process multiple subject files in one pipeline}
\usage{
process_subject_files(
  paths,
  subject_ids = NULL,
  read_fun = read_asc,
  read_args = list(),
  compute_measures = TRUE,
  compute_args = list(),
  parallel = FALSE
)
}
\arguments{
\item{paths}{Character vector of input file paths (one path per subject).}

\item{subject_ids}{Optional character vector of subject identifiers. Must
have the same length as \code{paths} when provided. Defaults to file basenames
without extension.}

\item{read_fun}{Function used to read one file. Must take \code{path} as its first
argument and return a list.}

\item{read_args}{Named list of additional arguments passed to \code{read_fun}.}

\item{compute_measures}{Logical. If \code{TRUE} (default), computes eye-movement
measures per subject using \code{\link[=compute_eye_measures]{compute_eye_measures()}}.}

\item{compute_args}{Named list of additional arguments passed to
\code{\link[=compute_eye_measures]{compute_eye_measures()}}. Defaults can be overridden here.}

\item{parallel}{Logical. If \code{TRUE}, uses \code{future.apply::future_mapply()}.
Requires \code{future.apply} to be installed and a future plan to be set by the
caller.}
}
\value{
A named list with:
\describe{
\item{\code{subjects}}{Named list with one element per subject. Each element
contains \code{subject_id}, \code{raw} (reader output), optional \code{fixations},
and optional \code{measures}.}
\item{\code{samples}}{Combined samples table across subjects (if available).}
\item{\code{events}}{Combined events table across subjects (if available).}
\item{\code{fixations}}{Combined fixations table across subjects (if
available).}
\item{\code{word_boundaries}}{Combined ROI table across subjects (if
available).}
\item{\code{character_boundaries}}{Combined character ROI table across
subjects (if available).}
\item{\code{trial_db}}{Combined trial metadata across subjects (if
available).}
\item{\code{measures}}{Combined word-level measures across subjects (if
computed).}
}

All combined tables include a \code{subject} column.
}
\description{
Reads and processes many subject files using a single call. The function can
run sequentially or in parallel (via \pkg{future.apply}) and returns both
per-subject results and experiment-level combined tables.

By default, files are read with \code{\link[=read_asc]{read_asc()}}. The reader can be changed with
\code{read_fun} (for example to \code{\link[=read_eyetrack_asc]{read_eyetrack_asc()}}).

If \code{compute_measures = TRUE}, word-level measures are computed per subject
with \code{\link[=compute_eye_measures]{compute_eye_measures()}} whenever fixation and ROI data are available.
}
