% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_fixations.R
\name{detect_fixations}
\alias{detect_fixations}
\title{Detect fixations from raw gaze samples}
\usage{
detect_fixations(
  samples,
  min_duration = 100,
  max_dispersion = 25,
  trial_col = "trial",
  eye_col = "eye",
  method = c("idt", "saccades")
)
}
\arguments{
\item{samples}{A data frame of raw gaze samples with (at minimum) columns:
\itemize{
\item \code{time}  – integer timestamp in milliseconds.
\item \code{x}     – horizontal gaze position in pixels.
\item \code{y}     – vertical gaze position in pixels.
}}

\item{min_duration}{Numeric scalar.  Minimum fixation duration in
milliseconds.  Defaults to \code{100}.}

\item{max_dispersion}{Numeric scalar.  Maximum allowed dispersion (radius
in pixels) within a fixation window; computed as half the sum of the
x-range and y-range.  Only used when \code{method = "idt"}.  Defaults to \code{25}.}

\item{trial_col}{Character scalar or \code{NULL}.  Name of the column that
identifies trials.  When supplied, fixation detection is run separately
within each trial.  Defaults to \code{"trial"}.}

\item{eye_col}{Character scalar or \code{NULL}.  Name of the column that
identifies which eye each sample belongs to.  When supplied, fixation
detection is run separately for each eye.  Defaults to \code{"eye"}.}

\item{method}{Character scalar.  Fixation detection algorithm to use.
\code{"idt"} (default) uses the built-in I-DT algorithm.  \code{"saccades"} uses
the \pkg{saccades} package (must be installed separately).}
}
\value{
A \link[tibble:tibble]{tibble} with one row per detected fixation and
columns:
\describe{
\item{\code{start_time}}{Integer.  Timestamp of the first sample in the
fixation (ms).}
\item{\code{end_time}}{Integer.  Timestamp of the last sample in the
fixation (ms).}
\item{\code{duration}}{Integer.  Fixation duration in ms
(\code{end_time - start_time}).}
\item{\code{avg_x}}{Double.  Mean x position during the fixation (px).}
\item{\code{avg_y}}{Double.  Mean y position during the fixation (px).}
\item{\code{n_samples}}{Integer.  Number of samples contributing to the
fixation.  (\code{NA} when \code{method = "saccades"}.)}
}
If \code{trial_col} or \code{eye_col} are provided and present in \code{samples}, those
columns are also included in the output.
}
\description{
Applies a \strong{dispersion-threshold identification (I-DT)} algorithm to a
sequence of raw gaze samples and returns a data frame of detected
fixations.  The algorithm groups consecutive samples into a fixation
candidate whenever the spatial dispersion (max – min of x and y within a
sliding window) falls below \code{max_dispersion}, with the window containing
at least \code{min_duration} milliseconds of data.

The I-DT algorithm was originally described in:
Salvucci, D. D., & Goldberg, J. H. (2000). Identifying fixations and
saccades in eye-tracking protocols.  \emph{ETRA}, pp. 71–78.

An optional \code{"saccades"} method is available when the
\href{https://github.com/tmalsburg/saccades}{saccades} package is installed.
That package implements a different velocity-based fixation-detection
approach.  Pass \code{method = "saccades"} to use it; an error is raised if
the package is not available.
}
\examples{
samples <- dplyr::tibble(
  time  = seq(0L, 990L, by = 10L),
  x     = c(rep(300, 60), rep(600, 40)) + stats::rnorm(100, 0, 2),
  y     = c(rep(400, 60), rep(400, 40)) + stats::rnorm(100, 0, 2)
)
fixations <- detect_fixations(samples, min_duration = 100, max_dispersion = 25)
print(fixations)
}
