% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categorize_fixation.R
\name{categorize_fixation}
\alias{categorize_fixation}
\title{Categorize Fixations as First-Pass or Other}
\usage{
categorize_fixation(sequence)
}
\arguments{
\item{sequence}{Integer (or numeric) vector of word numbers, one entry per
fixation in temporal order.}
}
\value{
A character vector the same length as \code{sequence} with values
\code{"first pass"}, \code{"first pass refixation"}, or \code{"other fixation"}.
}
\description{
Classifies each element of a word-number sequence into one of three
categories used in reading-research eye-movement analysis:
}
\details{
\itemize{
\item \strong{\code{"first pass"}} – The first time the eye lands on a word, before
moving to any later word.
\item \strong{\code{"first pass refixation"}} – A repeated fixation on the same word
immediately following a first-pass fixation on that word (no intervening
fixation on a different word).
\item \strong{\code{"other fixation"}} – Any fixation that is not a first-pass fixation
or first-pass refixation (e.g., a regression or a second-pass fixation).
}
}
\examples{
# Simple left-to-right reading
categorize_fixation(c(1, 2, 3, 4))

# Regression back to word 2 after word 4
categorize_fixation(c(1, 2, 3, 4, 2))

# Refixation on word 3
categorize_fixation(c(1, 2, 3, 3, 4))
}
