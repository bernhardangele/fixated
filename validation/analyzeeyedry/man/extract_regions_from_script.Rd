% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyzeeyedry.R
\name{extract_regions_from_script}
\alias{extract_regions_from_script}
\title{Analyze eye movement data using eyedry algorithms}
\usage{
extract_regions_from_script(
  filepath,
  delimiter = "^",
  lowest_cond = 1L,
  highest_cond = 999L,
  spillover_gap = 1L,
  spillover_size = 20L,
  multiline_offset = 160L
)
}
\arguments{
\item{filepath}{Path to the delimited script file.}

\item{delimiter}{Single character used as the region delimiter
(default \code{"^"}).}

\item{lowest_cond}{Minimum condition number to include (default 1).}

\item{highest_cond}{Maximum condition number to include (default 999).}

\item{spillover_gap}{Number of blank character positions between the end
of the last inline region and the start of the
trailing spillover region (default 1).}

\item{spillover_size}{Width in characters of the trailing spillover region
(default 20).}

\item{multiline_offset}{Character position offset for each additional
display line (default 160).}

\item{asc_files}{Character vector of paths to EyeLink ASC files, or
\code{NULL} (default).}

\item{da1_files}{Character vector of paths to DA1 files, or \code{NULL}
(default).}

\item{cnt_file}{Path to the CNT region-boundary file (required).}

\item{shorttime}{Fixation duration threshold (ms) below which fixations
are treated as "short" and ignored (default 80).}

\item{longtime}{Fixation duration threshold (ms) above which the region
on that trial is discarded (default 800).}

\item{fpcutoff}{Cumulative first-pass time cutoff in ms (default 2000).}

\item{ttcutoff}{Cumulative total-time cutoff in ms (default 4000).}

\item{width}{Character-grid width for linearising (x, y) positions
(default 80).}

\item{max_y}{Maximum y coordinate; fixations with y >= max_y are
discarded (default 1000).}

\item{nregmax}{Number of region columns to include in the output
(default 7).}
}
\value{
A \code{\link[tibble]{tibble}} with one row per subject × item ×
  condition combination.  Columns:
  \describe{
    \item{subj}{Subject number (integer; file order).}
    \item{item}{Item number.}
    \item{cond}{Condition number.}
    \item{seq}{Presentation sequence number.}
    \item{ff_R1 ... ff_R\{nregmax\}}{First fixation duration (ms).}
    \item{fp_R1 ... fp_R\{nregmax\}}{First pass / gaze duration (ms).
      Corresponds to GD.IXS in eyedry output.}
    \item{tt_R1 ... tt_R\{nregmax\}}{Total reading time (ms).
      Corresponds to TVT.IXS in eyedry output.}
    \item{oreg_R1 ... oreg_R\{nregmax\}}{Regression out (0 = no, 1 = yes).}
    \item{ireg_R1 ... ireg_R\{nregmax\}}{Regression in (0 = no, 1 = yes).}
    \item{pfix_R1 ... pfix_R\{nregmax\}}{Probability of first-pass fixation
      (0 or 1).}
    \item{nfix_R1 ... nfix_R\{nregmax\}}{Number of first-pass fixations.}
    \item{sp_R1 ... sp_R\{nregmax\}}{Second pass time (ms).}
    \item{gp_R1 ... gp_R\{nregmax\}}{Go-past time (ms).}
  }
  \code{NA} is used when there was no valid fixation in a region
  (\code{NV = 0} in eyedry) or when the region does not exist for
  the item / condition.

A data frame with the same column structure as \code{\link{read_cnt}}:
  \code{item}, \code{cond}, \code{nreg}, \code{end_last}, and
  \code{start_R1} through \code{start_R\{n\}}.
}
\description{
Reads one or more DA1 or ASC files together with a CNT region-boundary
file, computes all standard reading-time measures for every trial, and
returns the results as a wide-format tibble that is compatible with
eyedry's IXS output format.
}
\examples{
\dontrun{
result <- analyze_eyedry(
  da1_files = "example/comas/C1L1.da1",
  cnt_file  = "example/comas/frases_c.cnt",
  shorttime = 80,
  longtime  = 800
)
# First pass time for item 1, condition 1, region 1:
subset(result, item == 1 & cond == 1)$fp_R1  # should be 1250
}
\dontrun{
regions <- extract_regions_from_script(
  "example/comas/frases_comas_delimited.script",
  lowest_cond = 1, highest_cond = 2
)
}
}
