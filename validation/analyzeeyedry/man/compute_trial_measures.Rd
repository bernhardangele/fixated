% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyzeeyedry.R
\name{compute_trial_measures}
\alias{compute_trial_measures}
\title{Compute all eyedry measures for one trial}
\usage{
compute_trial_measures(
  fix_df,
  region_starts,
  region_end_last,
  shorttime = 80L,
  longtime = 800L,
  fpcutoff = 2000L,
  ttcutoff = 4000L,
  max_y = 1000L,
  width = 80L
)
}
\arguments{
\item{fix_df}{Data frame with columns x, y, s (start time), e (end time).}

\item{region_starts}{Integer vector of region start positions (character units,
0-indexed). Length equals the number of regions.}

\item{region_end_last}{End position of the last region (character units).}

\item{shorttime}{Fixations with duration <= shorttime are "short" and
ignored (default 80 ms).}

\item{longtime}{Fixations with duration > longtime cause the containing
region on that trial to be discarded (default 800 ms).}

\item{fpcutoff}{If cumulative first-pass time exceeds this, discard
region (default 2000 ms).}

\item{ttcutoff}{If cumulative total time exceeds this, discard region
(default 4000 ms).}

\item{max_y}{Maximum y coordinate; fixations with y >= max_y are
discarded (default 1000).}

\item{width}{Character-grid width: position = x + y * width
(default 80).}
}
\value{
Named list of numeric vectors, each of length \code{length(region_starts)}.
  Elements: ff, fp, tt, oreg, ireg, pfix, nfix, sp, gp. NA indicates no
  valid data for that region on this trial.
}
\description{
Implements the exact algorithms from eyedry.c (Clifton, 1990) for:
first fixation (ff), first pass / gaze duration (fp), total time (tt),
regression out (oreg), regression in (ireg), probability of first-pass
fixation (pfix), number of first-pass fixations (nfix), second pass (sp),
and go-past time (gp).
}
